
def Naive(topology_map, point, highest_offsets):
    pass



class Fancy(object):
    """
    Fancy strategy needs to keep track of its state, so it is created as a functor, meaning it
    can be called like a function, but can also store state.
    """
    def __call__(self, topology_map, point, highest_offsets):
        pass



